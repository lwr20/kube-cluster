apiVersion: v1
kind: ReplicationController
metadata:
  name: backend 
spec:
  replicas: 2
  template:
    metadata:
      labels:
        tier: backend 
    spec:
      containers:
      - name: nginx 
        image: caseydavenport/probe
        ports:
        - containerPort: 9000 
